syntax = "proto3";

message UserRequest
{
    int32 id = 1;
}

message UserResponse
{
    string name = 1;
}

service UserService
{
    rpc GetUser(UserRequest) returns (UserResponse);
}